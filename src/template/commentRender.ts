export const commentRender = `<%= Array.from({ length: indentLength }).map((item) => " ").join(""); %>/**
<%= Array.from({ length: indentLength }).map((item) => " ").join(""); %> * <%= description; %>
<%= Array.from({ length: indentLength }).map((item) => " ").join(""); %> */
`
